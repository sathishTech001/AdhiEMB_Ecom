# 00.7 Folder Structure — AdhiEMB Enterprise Guide

Backend (`adhiemb-backend`):
`com.adhiemb`: `common`, `config`, `exception`, `security`, `module` (12 modules).

Frontend (`adhiemb-frontend`):
`src`: `components`, `features`, `layouts`, `styles`, `types`.
